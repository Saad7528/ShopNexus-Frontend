// Bundle Models
export interface IComboBundle { id: string; title: string; badge: string; items: string[]; regularPrice: number; comboPrice: number; savingsBDT: number; status: 'ACTIVE' | 'EXPIRED'; }
